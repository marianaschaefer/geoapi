# --- Ambiente Virtual ---
.venv/
venv/
ENV/

# --- Cache do Python ---
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.ipynb_checkpoints/

# --- Banco de Dados Local ---
instance/
*.db
*.sqlite

# --- DADOS PESADOS (SENTINEL-2 & PROJETOS) ---
# Bloqueia recursivamente tudo dentro da pasta de projetos
SENTINEL2_BANDAS/projects/
# Bloqueia qualquer arquivo de dados solto na pasta raiz de bandas
SENTINEL2_BANDAS/*.tif
SENTINEL2_BANDAS/*.tiff
SENTINEL2_BANDAS/*.parquet
SENTINEL2_BANDAS/*.geojson
SENTINEL2_BANDAS/*.gpkg
SENTINEL2_BANDAS/*.csv
SENTINEL2_BANDAS/*.shp
SENTINEL2_BANDAS/*.dbf

SENTINEL2_BANDAS/
!SENTINEL2_BANDAS/.gitkeep  # (Opcional) Se quiser manter a pasta vazia no GitHub
# --- Configurações Locais e Sensíveis ---
.env
.flaskenv
.vscode/
.idea/

# --- Backups e Logs ---
*.log
*.backup.*
oldREADME.md
services/*.backup.*

# --- Docker ---
.dockerignore